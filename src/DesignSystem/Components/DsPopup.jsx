import React, { PureComponent } from 'react'
import PropTypes from 'prop-types'

export class DsPopup extends PureComponent {
  static propTypes = {
    dsBottomSheetProps: PropTypes.object,
    dsDialogProps: PropTypes.object
  }

  render () {
    return (
      <div />
    )
  }
}

export default DsPopup
